# project
_site
.sass-cache
.vagrant

# general
.DS_Store
Thumbs.db
ehthumbs.db
# Vendored gems for local preview only. GitHub Pages builds with its own
# pinned gems and never reads this directory or Gemfile.lock.
# .bundle/config is deliberately NOT ignored — it is tracked, and it carries
# the build flags that make `bundle install` work on this project.
vendor/
